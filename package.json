{
  "name": "relay-fragment-type-generator",
  "version": "0.1.0",
  "description": "Generates TypeScript interfaces for Relay fragments in source files.",
  "main": "dist/index.js",
  "author": "Eloy Durán <eloy.de.enige@gmail.com>",
  "license": "MIT",
  "bin": { "relay2ts": "dist/src/bin.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --outDir ./dist",
    "prepublish": "yarn build",
    "test": "yarn build && mocha dist/test/**/*.test.js"
  },
  "devDependencies": {
    "@types/graphql": "^0.8.6",
    "@types/mocha": "^2.2.37",
    "@types/node": "^7.0.0",
    "mocha": "^3.2.0",
    "strip-indent": "^2.0.0"
  },
  "dependencies": {
    "graphql": "^0.8.2",
    "typescript": "^2.1.5"
  }
}
